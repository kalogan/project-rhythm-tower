/** Placeholder — filled in by the Chart timeline slice. */
export function ChartsPanel(): JSX.Element {
  return (
    <div style={{ position: 'absolute', inset: 0, display: 'grid', placeItems: 'center', opacity: 0.7 }}>
      Chart timeline — building…
    </div>
  );
}
