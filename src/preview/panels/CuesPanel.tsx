/** Placeholder — filled in by the Cue & FX lab slice. */
export function CuesPanel(): JSX.Element {
  return (
    <div style={{ position: 'absolute', inset: 0, display: 'grid', placeItems: 'center', opacity: 0.7 }}>
      Cue &amp; FX lab — building…
    </div>
  );
}
